# Save Content — Full Instructions

此文件由 Phase 3A 加载。按以下 6 个步骤完整执行内容保存流程。

---

## Step 1: Content Acquisition

根据输入类型选择获取方式，**三类可叠加处理**（如"带图的微信文章"）。

### 1a. URL 抓取

**小红书**（域名含 xhslink.com 或 xiaohongshu.com）：
- 用 WebFetch 抓取页面 HTML
- 从页面 JSON 数据中提取 `"title"` 和 `"desc"` 字段（通常嵌在 `<script>` 标签内）
- 若页面含配图（笔记正文写在图片里），用 Claude 视觉能力逐图识别文字，拼接为完整正文
- 以 OCR 识别结果为主，`desc` 作为补充背景，**不要把两者都放入正文**（避免重复）

**微信公众号**（域名含 mp.weixin.qq.com）：
- 用 WebFetch 抓取页面
- 提取 `id="js_content"` 区域的文字，截断到 `id="js_article_comment"` 之前
- 同时提取 `og:title` 和 `id="js_name"` 作为标题和作者
- 若页面含"请在微信客户端打开" → 告知用户「该文章仅限微信内打开，请手动复制正文后发给我」，终止流程

**其他 URL**：
- 用 WebFetch 获取页面，提取主要正文区域
- 去除导航栏、广告、评论区、版权声明等噪音内容

### 1b. 图片识别

- 直接用 Claude 多模态能力读取图片中的文字
- 多张图片：逐一识别后按顺序拼接，用 `\n\n` 分隔

### 1c. 纯文本

- 直接使用用户提供的内容，无需额外处理

**获取失败时**：明确告知用户失败原因和替代方案，不要静默跳过。

---

## Step 2: Summary Generation

生成以下四项，**以对话形式展示给用户**（不是 JSON，不是代码块）：

```
📚 摘要

{一句话总结全文核心}

  • {要点1：原文中具体的观点/方法/数据}
  • {要点2：原文中具体的观点/方法/数据}
  • {要点3：原文中具体的观点/方法/数据}
  （视内容长度可有 4-5 条）

🏷️ #{标签1} #{标签2} #{标签3}
```

**质量红线**：
- 要点必须是原文里真实存在的具体内容，禁止写"本文介绍了…""这篇文章讲述了…"等空话
- 标签选 2-4 个大类（产品思维、AI应用、独立开发、职业发展、技术架构、创业、个人成长等）
- 若内容仅有标题+链接+分享口令，无实质正文 → 告知用户「内容不完整，请复制完整原文后重新发送」，终止流程

---

## Step 3: Find Related Notes

**加载 `prompts/knowledge-connect.md`**，传入以下内容执行关联笔记查找：
- 当前内容的一句话总结
- 当前内容的要点列表
- 今日文件名前缀（`YYYYMMDD_标题`，用于排除自身）

获得关联结果（可能为空）后进入 Step 4。

---

## Step 4: Ask for Reflection

在摘要和关联笔记之后，询问一句：

```
💭 你收藏这篇的原因是什么？（直接回复，或发「跳过」）
```

- 用户回复了实质内容 → 记录为 `reflection`，进入 Step 5
- 用户回复「跳过」或内容无关 → `reflection = ""`，进入 Step 5

---

## Step 5: Insight Generation

根据是否有 `reflection` 决定生成策略：

- **有 reflection**：启发必须围绕用户说的收藏原因展开，原文内容作为素材支撑
- **无 reflection**：基于原文最有价值的具体观点生成，不要泛泛而谈

综合以下信息生成：
- 原文内容（前 3000 字）
- 一句话总结 + 要点
- `reflection`（若有）

**输出格式**（严格遵守，不加额外标题）：

```
**具体启发**
- {启发点1}：基于原文具体观点的延伸，或结合收藏原因的具体思考
- {启发点2}：同上
- {启发点3}：如有第三条

**行动建议**
- {可立即执行的具体动作，不是"可以考虑"而是"做什么"}
- {可立即执行的具体动作2}
```

**质量红线**：
- 禁止出现"值得关注""可以思考""有一定启发"等空话
- 行动建议必须可执行（"下次做 X 决策前先做 Y 检验"而非"多思考这个问题"）
- 有 reflection 时，每条启发都应能与用户的收藏原因建立具体联系

---

## Step 5: Write to Obsidian

**文件路径**：`{vault_path}/{inbox_dir}/{YYYYMMDD}_{标题}.md`
- 标题去除特殊字符 `\ / : * ? " < > |`，截取前 30 字
- 若同名文件已存在，追加 `_1`、`_2` 依此类推

**写入以下模板**（完整内容）：

```markdown
# {标题}
{来源链接（若有，否则此行留空）}

## 总结
{一句话总结}

- {要点1}
- {要点2}
- {要点3}

## 启发

**具体启发**
- {启发点1}
- {启发点2}

**行动建议**
- {行动1}
- {行动2}

## 我的思考
{reflection 内容；若为空，写：（暂无）}

{若有关联笔记，插入以下区块，否则此处留空}
## 相关内容

- [[{笔记stem}|{笔记标题}]]：{一句话具体关联理由}
- [[{笔记stem}|{笔记标题}]]：{一句话具体关联理由}

## 原文

{完整原文正文}
```

**写入完成后**展示确认信息，然后执行 Step 6。

---

## Step 6: Confirm

写入完成后，展示确认信息：

```
✅ 已存入 Obsidian：{inbox_dir}/{文件名}.md
🏷️ #{标签1} #{标签2}
{若有关联笔记：
🔗 关联笔记：
  · [[笔记A]]：具体关联理由}
```
